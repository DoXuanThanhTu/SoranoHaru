import React from "react";

export default function Banner({ movie }: { movie: any }) {
  return (
    <div className="mb-8 w-full flex justify-center min-h-10">
      {/* <img
        src={movie.thumbUrl}
        alt={movie.name}
        className="w-full max-w-6xl rounded-xl shadow-lg"
      /> */}
    </div>
  );
}
